<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.6</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>dev.stockman</groupId>
	<artifactId>spring-cloud-azure-function-java21</artifactId>
	<version>1.0</version>
	<name>spring-cloud-azure-function-java21</name>
	<description>Example spring cloud serverless function to run on azure</description>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>25</java.version>
		<start-class>dev.stockman.functions.MainApplication</start-class>
		<appName>grayduck</appName>
		<functionAppName>${appName}-app</functionAppName>
		<functionResourceGroup>${appName}-group</functionResourceGroup>
		<functionAppRegion>centralus</functionAppRegion>
		<functionAppServicePlanName>${appName}-asp</functionAppServicePlanName>
		<functionPricingTier>Flex Consumption</functionPricingTier>
		<storageAccountName>${appName}storage</storageAccountName>
		<runtime.os>linux</runtime.os>
		<functionPort>7072</functionPort>
		<functionExtensionVersion>~4</functionExtensionVersion>
		<azure-functions-maven-plugin.version>1.41.0</azure-functions-maven-plugin.version>
		<azure-functions-java-library.version>3.3.0</azure-functions-java-library.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-function-dependencies</artifactId>
				<version>5.0.1</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
<!--			<dependency>-->
<!--				<groupId>com.microsoft.azure.functions</groupId>-->
<!--				<artifactId>azure-functions-java-library</artifactId>-->
<!--				<version>${azure-functions-java-library.version}</version>-->
<!--			</dependency>-->
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-function-adapter-azure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.microsoft.azure</groupId>
				<artifactId>azure-functions-maven-plugin</artifactId>
				<version>${azure-functions-maven-plugin.version}</version>
				<configuration>
					<appName>${functionAppName}</appName>
					<resourceGroup>${functionResourceGroup}</resourceGroup>
					<region>${functionAppRegion}</region>
					<appServicePlanName>${functionAppServicePlanName}</appServicePlanName>
					<storageAccountName>${storageAccountName}</storageAccountName>
					<pricingTier>${functionPricingTier}</pricingTier>
					<hostJson>${project.basedir}/src/main/azure/host.json</hostJson>
					<localSettingsJson>${project.basedir}/src/main/azure/local.settings.json</localSettingsJson>
					<funcPort>${functionPort}</funcPort>
					<runtime>
						<os>${runtime.os}</os>
						<javaVersion>${java.version}</javaVersion>
					</runtime>
					<appSettings>
						<property>
							<name>FUNCTIONS_EXTENSION_VERSION</name>
							<value>${functionExtensionVersion}</value>
						</property>
						<property>
							<name>MAIN_CLASS</name>
							<value>${start-class}</value>
						</property>
					</appSettings>
				</configuration>
				<executions>
					<execution>
						<id>package-functions</id>
						<goals>
							<goal>package</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>